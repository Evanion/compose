{
  "name": "@evanion/compose",
  "description": "React component that allows you to get out of Provider hell",
  "repository": "https://github.com/Evanion/compose",
  "private": false,
  "version": "1.0.2",
  "main": "./lib/cjs/index.js",
  "module": "./lib/esm/index.js",
  "types": "./lib/esm/index.d.ts",
  "files": [
    "lib",
    "README.md"
  ],
  "license": "MIT",
  "keywords": [
    "react",
    "compose",
    "provider",
    "context"
  ],
  "scripts": {
    "build": "yarn build:esm && yarn build:cjs",
    "build:esm": "tsc",
    "build:cjs": "tsc --module commonjs --outDir lib/cjs",
    "preversion": "yarn build",
    "publish": "yarn build && yarn publish:tag",
    "publish:patch": "yarn publish:tag && npm version patch && yarn publish",
    "publish:minor": "yarn publish:tag && npm version minor && yarn publish",
    "publish:tag": "git push --follow-tags origin master && npm publish"
  },
  "devDependencies": {
    "@types/react": "^18.2.6",
    "typescript": "^5.0.4"
  },
  "peerDependencies": {
    "react": ">16"
  }
}
